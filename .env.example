# Copie este arquivo para um novo arquivo chamado ".env"
# e preencha com suas credenciais reais.

# --- Credenciais do Banco de Dados (Exemplo) ---
DB_HOST=your_database_host_ip
DB_USER=your_database_user
DB_PASSWORD=your_super_secret_password
DB_NAME=your_database_name

# --- Caminho para os Ativos de Treinamento (Exemplo) ---
# Use barras normais (/), o script lida com a conversão.
# Exemplo Windows: C:/Users/YourUser/Desktop/Trainings
# Exemplo Rede: //network-share/trainings
TRAINING_ASSETS_BASE_PATH=//network_share/path/to/assets

# --- Configurações da API do Movidesk (Exemplo) ---
MOVIDESK_API_TOKEN=your_movidesk_api_token

# IDs de campos personalizados específicos da sua instância
MOVIDESK_VERSION_FIELD_ID=64221
MOVIDESK_OTHER_FIELD_ID=2529
MOVIDESK_OTHER_FIELD_RULE_ID=731

# --- IDs de Agentes/Equipes do Movidesk (Exemplo) ---
MOVIDESK_OWNER_ID=your_agent_id_like_2713614652
MOVIDESK_OWNER_TEAM_NAME=YourTeamName
MOVIDESK_ACTION_CREATOR_ID=your_generic_api_user_id_like_XXXXX

# --- Assinatura de E-mail para Ações do Movidesk (Exemplo) ---
# Pode ser HTML. Use aspas duplas se o conteúdo tiver espaços.
ACTION_HTML_SIGNATURE="<p>Atenciosamente,<br>Equipe de Treinamento</p>"